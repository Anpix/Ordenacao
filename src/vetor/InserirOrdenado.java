package vetor;

// @author Anpix

import java.util.List;

public class InserirOrdenado<T> {
    
    /*
    public InserirOrdenado(List<T> vet, int n, T e){
        if (n < vet.size()){
            int i = n;
            while(vet[i].chave > e.chave) and (i > 0){
                
            
            }
        }
    }
    */
}
